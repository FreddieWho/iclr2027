# 已授权的单次 C1 24,576-token 探针

日期：2026-09-11。结论：`NO_GO/TECHNICAL` 保持。唯一一次探针已完成，但 C1 仍无可见 memory；完整 P0、P1/P2 和科学假设验证均未完成。没有重试、截断、隐藏 reasoning 回填或科学评分，不自动继续增加输出额度。

## 授权与比较条件

用户明确同意上一轮提出的单次探针。授权记录为 `configs/c1_output_budget_authorization_24576.json`；原 `configs/c1_output_budget_proposal.json` 保留为未批准时的历史快照。此次授权仅限一次，不等于全网格采用新额度。

恢复原始32K history `syn-20260910-00000` 与原始 `prompts/compress_neutral.txt`，不使用上一轮16K数据或长度校准提示。模型仍为 OpenCode Go / `glm-5.3-flash`，`reasoning_effort=max`、temperature=0.2、top_p=0.95；唯一模型请求参数变化是 provider 总输出由10,240提高到24,576。memory仍≤1,024 native tokens并须自然停止；R1合同未变。独立session仅接收当前history，未来问题/gold未读入压缩器。

## 实测结果

| 项目 | 实测 |
|---|---|
| run start UTC | 2026-09-11T15:27:19.550136+00:00 |
| returned model | glm-5.3-flash |
| history native tokens | 32,747 |
| full request native tokens | 32,938 |
| provider input / output tokens | 32,951 / 24,576 |
| provider reasoning tokens | 24,553 |
| cache read tokens | 0 |
| visible memory tokens | 0；content为空字符串 |
| finish reason | length |
| latency | 215.108秒 |
| 状态 | compressor_non_natural_stop；scientific_score=null |
| 本次实测费用 | $0.01723065（USD-equivalent Go usage） |

输出总量与reasoning之差23 tokens不能当作可见memory；本地冻结tokenizer对实际content计数为0。该结果只说明在这条原始32K输入/提示与固定生成合同下，24,576总输出仍不足以取得合格memory；不能证明所有输入必然失败，也不能判断direct/staged的效应。

GLM tokenizer完整revision：`eb9eb208eb0d988989d07a6a12d0fdeb5f52574a`。原模型列表快照SHA256：`e859652c6d13b5233309d8f53d0525325225ba4300ecabe7bb8cc0d31967cffd`；本轮未重新GET模型列表。gateway精确上限仍未公开，本地65,536检查不冒充实测gateway上限。

## 费用与一次性保护

共享账本仍为 `work/p0_repair_accounting_v1/cost_ledger.sqlite`。本次前累计保守占用$0.15204730，加一次已测费用$0.01723065，累计$0.16927795，P0剩余额度$0.33072205。累计包含历史不确定费用预留，不全是已知实付。

本次账本从5行变为6行，其中1行是历史费用结转，不是API请求；本轮新增API尝试恰为1。请求总数上限锁为既有行数+1、transport_attempts=1，输出目录拒绝复用。请求后再次运行仅本地preflight，被 `one-shot authorization already consumed` 拒绝，没有发送第二次请求。

## 可追溯产物与继续边界

- `artifacts/c1_24576_probe_result.json`：授权hash、run manifest、完整非敏感usage/字段形状、模型/预算/可见长度与费用。
- `work/p0_c1_24576_authorized_v1/`：独立request config、run manifest、result、cache和费用快照；cache不保存隐藏reasoning正文。
- `scripts/probe_c1_24576.py`：一次性入口；本次只补充授权注入和原始提示tokenizer填充，未改模型参数合同或评分规则。6项相关传输/账本回归测试通过；前轮完整50项测试记录保留，本轮未重复全套。

已执行命令（授权已消耗，不可当作继续调用指令）：

```bash
/opt/anaconda3/bin/python3 scripts/probe_c1_24576.py --preflight-only
/opt/anaconda3/bin/python3 scripts/probe_c1_24576.py
```

停止该C1适配路线的自动尝试。现有剩余美元额度不授权继续扩大token/prompt搜索，也不能用同一失败配置重跑网格。若未来采用其他修复，须给出新的可检验技术依据和明确范围，并在P1前对所有路径统一冻结；不改变或混用当前未产生的科学结果。
