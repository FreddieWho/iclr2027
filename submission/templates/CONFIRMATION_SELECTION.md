# 探索→确认交接

探索run/data/model/prompt hashes：
确认history生成seed或独立数据group规则：
候选主假设（至多两个）：
预设比较/预算/类型/方向：
数值门槛及确认CI family size：
为什么选择（可以基于探索，不能假称先验）：
什么结果仍会NO_GO：
最多一轮扩展的触发条件：

这是项目记录，不需要外部预注册。确认集看过之后发生的变更另写为探索。
