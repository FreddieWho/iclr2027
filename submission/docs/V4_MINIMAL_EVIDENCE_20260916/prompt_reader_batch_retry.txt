PROTOCOL RETRY. Return exactly one valid JSON object with an "answers" array containing one object for every supplied question, with keys "question_id", "answer", and "evidence_ids". Preserve question IDs exactly. No markdown or extra text. Each answer must be a short phrase (at most five words where possible); include at most one supporting evidence ID when available. Do not include reasoning. The entire visible response must be at most {visible_limit} native tokens under tokenizer {tokenizer}. Use only the supplied memory; if it is insufficient, answer "UNKNOWN".
