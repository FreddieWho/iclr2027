\section{External Confirmation on an Independent Provider}
% ROLE: ~1 page. The differentiator (top reviewer pain point: external
% validity).

% SUBSEC 6.1 — Data: SoccerTrack-v2 (CC BY 4.0, pinned revision), 8 matches
% (smoke matches excluded by pre-frozen rule), 44,401 snapshots / 7,534
% natural pairs; same conversion rules, response-blind thresholds; per-half
% team-side flips caught by the pre-registered identity-alignment logic.
% Provider shift (professional league → university league, different
% tracking system) IS the distribution-shift test.

% SUBSEC 6.2 — Task confirmation (TABLE = reports/P3_HEADLINE_EFFECT_TABLE.md):
% four primary metrics 8/8 (sign test p=0.0039, match-level bootstrap CIs):
% zone +0.0054 [+0.0042,+0.0073]; pair +0.0092 [+0.0064,+0.0115];
% geometry +0.0334 [+0.0280,+0.0394]; centroid MAE -34% [+0.0177,+0.0201];
% auxiliary match-half -0.0827 (0/8, reported — Sec.5.4).
% WORDING RULE: "small effects, consistent direction" — no magnitude
% inflation. Weakest match (132877, full=0.421) stays in the table.

% SUBSEC 6.3 — Mechanism confirmation: per-match intervention full above
% spectral baseline 8/8 (macro gap ≈ +0.53), direction accuracy 0.66-0.73.
% Claim boundary (ledger): single independent provider ≠ cross-domain
% generalization; 2:1 numerics ≠ universally optimal ratio.

% [SLOT: AMR positioning — PENDING USER DECISION (options A/B/C,
% D-20260905-T5R6-002). Default per agent recommendation (A): one outlook
% paragraph — Action-Mode Routing as gated future work, differentiated from
% Partial G-CNNs (must cite) by routing on MEASURED response profiles.]
