\section{Introduction}
% ROLE: 1 page. Sell the question, not the domain.
% MUST READ AS: general representation-learning contribution with soccer as
% the instrumented testbed (per landscape brief: "sport is the evaluation
% domain, not the contribution").

% PARA 1 — The gap. Invariance is usually defined per transformation OPERATOR
% (translation, rotation). In multi-entity systems the semantics depend on how
% the operator is DISTRIBUTED over entities: whole-team advance vs back-line
% advance vs single-player step can have identical magnitude and different
% meaning. Index = operator x support x task (blueprint Sec.1).

% PARA 2 — What we do. (i) A controlled instrument: equal-energy displacement
% reallocated across node supports (Sec.2). (ii) A phenomenon: responses are
% architecture-dependent yet predictable from normalized local geometry
% (Sec.3-4). (iii) A repair: task-semantic channel split with asymmetric
% update allocation, confirmed on an independent provider (Sec.5-6).

% PARA 3 — Contributions (bullets):
%  - support-reallocation probing protocol (novel instrument; position vs
%    Morris elementary effects / budgeted attacks / COMA counterfactuals —
%    see method-positioning brief (b): "novel combination, not novel
%    perturbation")
%  - local-geometry predictor with QUANTIFIED validity regime; honest margins
%    over baselines (ledger P3-R10/R14)
%  - evidence that coalition semantics live in coupling blocks and grow with
%    coalition size (P3-R12); phenomenon is training-induced (P3-R11) and
%    objective-family-robust (P3-R13)
%  - externally confirmed repair with measured selective cost (P3-R9)
%  - evaluation protocol: pre-registered-style locks, single-read holdout,
%    full artifact hashing (Sec.8)

% FIGURE 1 HERE: concept figure — same-energy displacement on three supports
% (whole team / line / single) → different representation responses; plus
% geometry→response prediction scatter. [TODO: figure generation]
