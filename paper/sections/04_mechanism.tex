\section{Endpoint Repair, Migration, and Relational Input}
\label{sec:repair}

\paragraph{Component improvements.} The repair is diagnostic, not a
product: training on oracle-mined boundary-crossing variations (flipmine)
tests whether exposing the model to the missing update shape changes
behavior. On the fresh holdout: single-flip C1 miss $52.4\% \to 34.7\%$
(paired same-sample comparison; C0 false alarm $26.2\% \to 23.8\%$, within
$\pm 5$pp, so no alarm inflation); action invalidity at $\lambda{=}2$,
$75.0\% \to 62.5\%$; turn miss $43.8\% \to 31.3\%$; and static error
\emph{improves}, $18.0\% \to 10.7\%$. The decision-interface decomposition
(Sec.~\ref{sec:interfaces}) locates much of the action gain in coverage
rather than precision, and an affine rescaling of clean logits fitted on
dev cannot reproduce the repair (paired affine-vs-clean comparison
intervals touch or cross zero). The repair is therefore not a pure
operating-point shift, within the boundedness stated for that control
(Sec.~\ref{sec:limits}). The same coverage-not-precision pattern holds at
the decision-boundary level: on $1301$ oracle single-crossings (dev pool,
frozen checkpoints, matching radius $r{=}0.025$), clean models place a
decision root near only $9.6\%$ of oracle boundary points; flip coverage
raises this to $16.9\%$ ($+7.3$pp, parent-cluster CI $[0.020,0.128]$) with
offset and signed alignment unchanged---a boundary-coverage shift, not
boundary alignment (discovery-grade, no fresh confirmation).

\paragraph{Endpoint repair is not full repair.} Targeted flip coverage
improves several component metrics, but paired quartet analysis on the
baseline-110 subset $H$ (Sec.~\ref{sec:setting}) reveals that endpoint
repair substantially overstates recovery of full compositional consistency.
On the dev bank (parent-cluster CIs; Fig.~\ref{fig:migration}): full
repair $0.03$--$0.10$; error migration $0.36$--$0.40$; and since
$R_{\mathrm{endpoint}} = R_{\mathrm{full}} + M$, migration accounts for
$79$--$94\%$ of the observed endpoint repair across seeds on this
raw-coordinate comparison. That share is not a claim that every repair
mostly migrates; the rendered ResNet-18 comparison is a boundary
(Sec.~\ref{sec:pixel}). Joint
consistency $J = P(111 \mid E)$ moves only slightly ($0.05 \to 0.06$--$0.08$):
a small change, and not yet evidence of a large joint-consistency gain
from raw flip coverage alone. On the common-start-correct subset the
repair direction is inconsistent across seeds (including one reversal,
s23 $0.920 \to 0.947$). A preserve-balanced retraining pilot with matched
loss weights does not reduce migration; this bounds \emph{that fixed}
sampling/weighting scheme, not positive-example protection in general.
For reference, the flipmine arm's own-conditional emergent rate on the
fresh holdout is $109/141 = 77.3\%$ on a different denominator ($141$ vs
$176$, non-paired)---descriptive only, not a repair delta.

\begin{figure}[t]
\centering
\includegraphics[width=0.78\linewidth]{figures/fig3_p3_migration.pdf}
\caption{Raw flip coverage only, baseline-110 subset $H$ from the 237-quartet
dev bank (106 parents; parent-cluster intervals). Migration carries
$79$--$94\%$ of this endpoint gain, not of every repair.}
\label{fig:migration}
\end{figure}

\paragraph{A bounded positive instance: relational input $\times$ flip
coverage.} Label-free relational input features change the picture where
flip coverage alone could not. The unified fallback
Table~\ref{tab:fourarmunified} reports the four archived development-bank
arms on the same 237-quartet/106-parent denominator; missing fields are
marked rather than copied from another bank. The separate 509-quartet
confirmation analysis remains in the appendix and is not pooled here.
On the development bank, relational input plus flip coverage reaches
$J_3$ 0.384--0.422 across the three archived seeds, while its full-repair
fraction is 0.364--0.442 and migration is 0.317--0.418 under the fixed
same-seed raw-clean baseline. These are bounded input/flow observations,
not a universal synergy law. A fresh-source strong-baseline check also
solved all 230 official E pairs (65 parents) with an analytic
segment-intersection parser ($J_3=1.0$). Thus the source learned-arm
comparisons are parser-bounded diagnostics, not evidence that a complex
learned relation model is needed; the visual parser comparison remains a
separate Route-2 question. The previously reported cross-bank interaction
and relabeling ranges are retained as appendix diagnostics, with their
denominators and bank separation intact.
A controlled follow-up
(App.~\ref{sec:f095}) supports geometric organization under the tested training recipe---six
pairwise distances, in the spirit of relation modules \citep{santoro2017simple}, suffice for the observed gain in this setting, and a depth-matched concatenation matches raw. The source-task typed-pair negative does not exclude learnable relational ability: segment-sum plus a linear readout cannot represent a general crossing (\texttt{artifacts/e832\_focus/structure/EXPRESSIVITY\_REPO\_AUDIT.json}). That file is not a repaired-model success, and it does not cover TypedTri or TypedDisk. These fixed-recipe results do not exclude optimization or establish that symmetry is irrelevant. Ordering analysis describes the final encoder--head score (Fig.~\ref{fig:fourarm}c):
the representation improves state separability $H$ ($0.54$--$0.57 \to
0.81$--$0.87$), while flip coverage on relational input mainly realizes
that separability at the fixed threshold rather than creating new
separability. This substantially improves joint consistency
and shifts repair toward full consistency; it does not solve the task
($J < 0.5$ leaves a majority of quartets inconsistent), and the features
are hand-designed and relation-aware by construction---an existence proof
for joint consistency under this input, not a general architecture
principle.

\paragraph{Cross-task advantage and interaction boundary.}
On point-in-triangle (T1; 2,724 E quartets from 322 parents),
distance+flip has $J=.749/.773/.727$, versus $.249/.210/.193$ for
raw+flip. On segment--disk (T2; 3,443 quartets from 369 parents), the
corresponding values are $.998/.997/1.000$ and $.606/.640/.643$
(seeds 11/23/47). T1 uses six distances and T2 three role-specified
distances. The original $J$-scale interaction is
$+0.3935/+0.4394/+0.4126$ on T1 but $-0.1891/-0.1719/-0.2504$ on T2; paired parent-bootstrap 95\% intervals exclude zero in the corresponding directions (App.~\ref{sec:f095}).
In the earlier U10 distance-versus-raw comparison, input advantage appeared
on both tasks; the new Route-1 rich-sorted-versus-rich-unsorted comparison
did not confirm that source ordering on T1/T2. Positive interaction does
not transfer in the earlier U10 comparison. The T2 interaction stays negative.
These are two task comparisons, not eighteen independent replications.
Indexed normalization invalidates the old target-task typed models'
exact-invariance claim. Corrected role-shared normalization and
retraining restore pipeline symmetry within floating-point tolerance;
typed+flip still trails distance+flip under the tested recipe
(App.~\ref{sec:f095}). That target-task fusion is not the source-task
witness and does not exclude learnable relational ability.

\paragraph{After matched optimization.} A separate source-task pool contains
1,229 quartets from 250 eligible parents. It is not the 237-quartet or
509-quartet bank, and it is not pooled with them. Across 108 candidates selected
only by static/single dev BCE, 4N raw+flip reaches $J=.334$--$.475$ while
distance+flip reaches $.819$--$.889$. Distance at 25\% of the retained
flip pool exceeds raw at 100\% in every size--seed cell (N/4N, three seeds).
This is finite-budget retained-example efficiency, not oracle-query savings
or exclusion of other optimizers (App.~\ref{sec:f095}).

% FIGURE SLOT (R6.4): main positive-result figure --- four-arm joint
% consistency bars (dev + confirmation) with repair-flow decomposition;
% generator owned by the figure lane; expected file figures/fig4_fourarm.pdf
\begin{figure}[t]
\centering
\includegraphics[width=0.82\linewidth]{figures/fig4_fourarm.pdf}
\caption{Four-arm comparison (dev: 237 quartets/106 parents; confirmation: 509 quartets): joint consistency $J$
(left), the full-repair/migration decomposition on $H$ (middle), and
fixed-threshold $J$ versus oracle separability $H$ on dev (right).}
\label{fig:fourarm}
\end{figure}

\input{sections/tab_fallback}
