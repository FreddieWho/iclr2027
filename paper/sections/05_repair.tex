\section{Task-Semantic Repair as a Test of the Diagnosis}
% ROLE: ~1.5 pages. The repair is a FALSIFIABLE TEST of the diagnosis, not a
% product (review Part 4-C). If the context/intrinsic conflation diagnosis is
% right, separating the channels should remove the pathology.

% SUBSEC 5.1 — Diagnosis restated (P3-R1): the old task gate conflated
% context signal with intrinsic nuisance; intrinsic objective must shape the
% SHARED encoder, not only a terminal head (P3-R6, Round-1 negative control
% routing_mode_head_only).

% SUBSEC 5.2 — The repair: dual-channel encoder (context channel sees raw
% deployment coordinates; intrinsic channel sees globally centered views),
% plus ASYMMETRIC UPDATE ALLOCATION on the shared encoder (2:1), selected by
% a bounded 2-round autoresearch under frozen rules (3:1 rejected for
% seed-level geometry instability; search closed; no Round 3).
% WORDING RULE: 2:1 is an INSTANCE of the principle "update allocation is a
% substantive variable" (P3-R7), not a tuned hyperparameter claim.
% [SLOT: ratio-sweep ablation 1:0/1:1 — PENDING USER DECISION (review 3.3);
% if approved, dev-only post-hoc ablation, no new test reads.]

% SUBSEC 5.3 — Single-read held-out confirmation (T5R5, J03WQQ):
% task gate all-pass (pair -0.0026, geometry +0.0051, context non-inferior);
% intervention full 0.63 vs baseline ~0; translation response near zero.
% WORDING RULE (review Part 4-B, MANDATORY): z_mode translation near-zero is
% BY CONSTRUCTION (input re-centered after translation) — present as design
% guarantee + sanity check; the empirical content is context-channel
% accessibility retention and the raw single-channel contrast (0.256).

% SUBSEC 5.4 — The measured cost (P3-R9, MUST appear alongside the wins):
% auxiliary match-half probe 0/8 against (F1 0.19 vs 0.27, -30%): update
% allocation costs SELECTIVE context retention — spatial context (zone,
% centroid) improves while temporal/period context degrades. This bounds the
% context-preservation claim and is itself evidence that allocation matters.
