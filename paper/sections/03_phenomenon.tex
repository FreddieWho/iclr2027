\section{The Phenomenon: Heterogeneous but Real Support Sensitivity}
% ROLE: ~1 page. Compressed P2 + training-induced evidence.

% PARA 1 — Fracture responses (P2 frozen facts): equal-energy reallocation
% produces non-trivial representation responses; exact-spectrum controls show
% graph-frequency power alone cannot explain them (P2-C1); direction/magnitude
% depend on architecture, graph, role, energy — frozen as
% mixed_or_graph_specific (P2-C2). HONEST NEGATIVE: no universal
% mid-frequency blind spot; role alliance not universally special (P2-C3).

% PARA 2 — Training-induced (P3-R11): untrained controls respond ~1e-6 vs
% ~1e-1 trained (5 orders of magnitude) — support sensitivity is CREATED by
% training. Untrained nets sit in the trivial linear regime (quad/actual ≈ 1);
% training amplifies responses and pushes the map out of it.
% WORDING RULE: untrained ΔR near float32 precision — use magnitude contrast
% only, do not interpret its Spearman absolute value.

% PARA 3 — Objective-family robustness (P3-R13): Barlow-Twins control (frozen
% 2:1 recipe, loss swapped) shows the same support sensitivity and geometry
% predictability (full rho 0.47-0.67, mean 0.59 ≈ contrastive 0.56).
% BOUNDARY: same architecture/views; architecture generality (EGNN-class)
% untested — Limitations.
