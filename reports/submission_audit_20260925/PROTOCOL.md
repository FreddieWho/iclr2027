# 最终投稿前结论与代码审查

日期：2026-09-25。起点：`3fc9757`；精确版本和环境见
`artifacts/submission_audit_20260925/START_RECEIPT.json`。

用户授权：检查各个结论及对应代码，修复影响结果的错误，补齐低成本或必要缺口，形成下一轮正文写作的证据目录。本轮不以完成论文为交付目标。

## 审查范围

| 分组 | 内容 |
|---|---|
| core | P1/P2/P3、原子条件、修复流、置信度和部署解释 |
| mechanism | M1 源表示、M2 冻结迁移及真实几何对照 |
| routes | e832 结构、路线 1–4、解析器与视觉 v2/v3 |
| vision | N02 升级、视觉控制和原生 VLM 边界 |
| controls | O01/O02/O04、血缘、自然足球 E 和全场景部署 |
| paper_map | 现稿主文/附录的结论覆盖和证据引用 |

每项结论记录：代码、结果、实际分母、选择和数据使用历史、独立复算、允许措辞、限制及尚未运行的计算。区分代码合同通过、归档数值一致、科学支持；未达到阳性标准不等于排除机制。

## 执行边界

- 优先使用已建索引的 CodeGraph。由六个 gpt-6-luna（max）分工审查，总控整合证据和处理交叉口径。
- 已冻结的旧脚本与历史结果保持原样；纠错复算写入本轮目录，保留新旧对应关系。
- 不重新访问封存原始池 `holdout_909`、`confirm_1007`、bridge `holdout_895`、`J03WQQ`、`SoccerTrack-v2`。可核查已发布汇总，但不能把它说成重新验证封存数据。
- 不新租 GPU、不新增付费服务、不公开发布、不操作退租。本轮首先使用已归档预测做 CPU 复算。
- 仅对实际修复做针对性回归检查。无需对文档和索引改动运行广泛测试。
- 数据集、parent、quartet、seed、请求、帧对分别计数；不得互充独立重复。
- 保留不足以补齐的计算为 `NOT_RUN` / `BLOCKED`，说明其对主张的影响。

## 交付

本目录的 `README.md` 为写作入口，`CLAIMS.json` / `CLAIMS.csv` 为合并结论表，`FINDINGS.md` 为修复与结论变化，`GAPS.md` 为剩余计算，`REPRODUCE.md` 为复算入口。分组审查保留在子目录；输入哈希、复算和运行回执位于 `artifacts/submission_audit_20260925/`。

## GPU 补算授权更新

用户在审查中提供既有远程 GPU 实例并授权必要使用。只读预检为 2080 Ti 11GB、torch 2.5.1+cu121、GPU 空闲，旧输入与权重可复用。最初计划补跑 M2 正确冻结/微调的 6 个前端臂；随后发现旧模型初始化早于设种子，故扩为完整 16 格，统一修正初始化和静态曝光合同，再补 route2 曝光匹配的 3 个静态基线。按旧回执 M2 训练约 30–80 秒/格，总体为十分钟级，不新增租机、不修改旧运行。输入、代码和回收结果逐文件校验。环境回执见 `GPU_AUTHORIZATION.json`，不含登录信息。
