# 预注册：confirm1007 重标号稳健性检查（C1c-confirm）

**写于运行之前**（2026-09-22，用户授权当日）。封存池新协议事件记录见
`CONFIRMATION_USAGE.md`。本文件在拿到任何 confirm 结果之前固定判据。

## 为什么做

dev512 上发现（`C1C_RELABELING_ROBUSTNESS.md`）：
1. U1 交互 I 在任务对称群的 8 个标号下 24/24 全正；
2. J 的标号敏感性按臂单调递减（raw_clean → relflip：1.15–1.33 → 0.25–0.29）。

若这两条在独立确认池上同向复现，则可安全写进正文（"交互在任务自身对称群下稳健，
且关系输入抑制标号敏感性"）；若不成立，则必须在正文加限定或只保留 dev 级措辞。

## 方法（与 dev 完全相同，不调参）

- 池：`bank_confirm1007.npz`，n_q = 509，Qx/Qe/Qmeta 结构同 dev；
- 标号：8 个群元素（AB 内交换 × CD 内交换 × 段交换），联合作用于 Qx 与 Qe；
- oracle 不变性先验证：每个四元组在 8 标号下的 yA/yB/yAB 必须完全相同
  （由构造保证，此处显式断言）；
- 臂：raw_clean / raw_flipmine / relfeat / relflip，冻结权重，无训练；
- 种子：11/23/47；
- 指标：每标号下 J（A 对 ∧ B 对 ∧ AB 对），以及
  I = (J_relflip − J_relfeat) − (J_rawflip − J_raw)。

## 预注册判据

**P1（主）**：I > 0 在 **全部 24 个**（3 种子 × 8 标号）组合成立
（每种子 8/8 且三种子全满足）。

**P2（次）**：J 的相对波动（极差/均值）在每个种子内满足
raw_clean ≥ relfeat ≥ relflip 的**弱序**（即 relflip 是四臂中标号最不敏感的），
三种子全满足。

**P3（反挑标号）**：论文使用的恒等标号 J 值**不得同时是 8 个标号中的最大值**
（若恒等恰为最大，需在正文声明）。

## 判决口径（现在固定，看数不改）

- P1 成立 → 交互稳健性可写正文，措辞"在任务对称群的全部 8 个标号下同向"。
- P1 不成立但恒等标号同向 → 只保留 dev 级措辞，confirm 仅作方向性注记。
- P2 成立 → "关系输入抑制标号敏感性"可写为机制句；不成立则降为 dev 观察。
- P3 不成立 → 在正文注明论文报的是分布中的上端值（诚实披露）。

## 不做的事

- 不重新选路径、不改 bank、不调阈值、不新增臂；
- 不把 8 个标号当作 8 个独立样本（区间为描述性范围）；
- 不覆盖任何已有产物（新目录 `l007_perm_u1_confirm/`）；
- 不含因果断言。
