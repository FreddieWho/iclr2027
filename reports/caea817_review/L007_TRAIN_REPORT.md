# L007 训练线报告 — P1 锁定动力学（阴性）＋ P2 LOO（阴性）

## P1：转折精度是全程漂移，不是早期锁定

输出 `artifacts/next_novelty/l007_lockin/L007_LOCKIN.json`（8 种子 × 13 检查点）。
- 中位锁定 epoch 287.5 vs 中位 loss 收敛 300 → 判据（前者<后者一半，≥6/8）未满足。
- 种子内排序稳定性单调爬升（ep25 时 0.15–0.58 → ep275 时 0.99+），全程无平台期。
- 跨种子一致性全程平坦 ~0.35–0.44（ep25 与终态无异）——种子间从未"收敛到同一答案"。
- 判决：drift/late。关键期解释关闭；转向晚期 margin 动力学的后路也不开（那是新训练线，需另行授权）。

## P2：TracIn 筛选有信号，精确 LOO 证伪数据决定论

筛选输出 `artifacts/next_novelty/l007_tracin/TRACIN.json`（代理=转折邻域 ±0.15
oracle 标注态 1794 个；7 检查点 × 2046 监督态 × 8 种子梯度点积）。
筛选本身有模样：11 个场景跨 ≥3 种子共享 top-10（416/100/411/204/91/207/85/83/301/419/150）。

LOO 输出 `artifacts/next_novelty/l007_loo/LOO.json`（删整场景=去其 clean 态＋全部 flips；
top-6 vs 随机-6 × 2001–2003，同 init 只差数据；36 次复训）：

| seed | top-k 中位\|Δ\| | 随机中位\|Δ\| | 比值 | top 中位符号 |
|---|---|---|---|---|
| 2001 | 0.0066 | 0.0077 | 0.85 | − |
| 2002 | 0.0239 | 0.0284 | 0.84 | ＋ |
| 2003 | 0.0024 | 0.0142 | 0.17 | ＋ |

判据（比值≥2 且三种子同号） decisively 未满足：删整场景只搬动 mean|terr| 千分之几，
与随机删除同量级。s2002 的 6/6 同号不追（量级在噪声地板内，且判据要求三种子一致）。
判决：少数点钉住边界已死；TracIn 分数降格为描述性统计，不再重训。
边界单删的稳健性本身与“噪声主导”一致：边界位置对单场景删除不敏感。

## P3：训练点几何钉位已死（含一次 bug 修复）

输出 `artifacts/next_novelty/l007_pin/PIN.json`（A 转折邻域插入 40 oracle 态；
B 删 20 最近场景；C sham 删 20 最远场景；2001–2004，同 init）：

| seed | A | B | C |
|---|---|---|---|
| 2001 | −0.10 | ＋0.07 | ＋0.06 |
| 2002 | ＋0.11 | ＋0.22 | ＋0.11 |
| 2003 | −0.03 | ＋0.03 | ＋0.06 |
| 2004 | −0.21 | −0.15 | −0.11 |

判据（A 降≥10% 占 ≥3/4 且 C 变动<5% 占 ≥3/4）：A 仅 2/4，C 0/4——
连 sham 都搬动 ±6–11%，干预组的变化淹没在噪声里。判决：钉位已死。
插曲：第一版 B/C 四种子逐位相同，系最近邻距离矩阵 reshape 压成标量所致
（argsort(标量)=[0]，两组删了同一场景）；已修＋加“近/远集合不交”断言，重跑收敛。
正是 sham 对照的存在让 bug 无处藏身。
