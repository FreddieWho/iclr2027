> **[历史文档 · 2026-09-24 补注]** 本文件是租机前的方案与价格调研，其中的 NOT_RUN 与推荐机型均为当时状态。
> 用户后自行提供 RTX3080Ti 12GB 实例，未新增租机；全部 GPU 任务已完成并回收校验。现行状态见 `R_FIXES_REPORT.md`。

# 视觉GPU迁移方案（仅规划，未租用）

> 状态更新：用户已提供远程实例并授权执行，完整GPU矩阵现已启动；本文件保留原规划/CPU历史，当前以 [REMOTE_GPU_EXECUTION.md](REMOTE_GPU_EXECUTION.md) 为准。
2026-09-24，用户允许视觉训练耗时过长时规划GPU实例训练。当前授权是方案准备；未创建实例、未付费、未上传数据、未停止CPU任务。

## 当前实测与需求

CPU正式run为 `artifacts/e1a933_review/vision_canonical224_v2/`，8线程，batch32，20epochs，3seed×7臂=21次训练。首epoch73.24秒，完整矩阵估计9–12小时。此为运行期估计，非完成承诺。2816训练图像对应88steps/epoch，全矩阵36960优化步骤；开发评估、辅助诊断及末次测试另计。

推荐单卡24GB NVIDIA GPU，4090优先、3090作为低价备选，8CPU、32GB内存、约40GB系统盘（准确容量以传输清单加环境安装余量核定）。ResNet18无需为本轮默认选多卡或80GB显存。保持batch32、FP32、样本/顺序/随机流/epoch和dev选模规则，先不启用AMP或改变训练配方。

GPU时间未实测。示例容量预算：若完整epoch能降至15–25秒，420epochs约1.75–2.92小时，另加环境/传输/测试时间；这只是计划假设，不能声称已获3–5倍加速。实例可用后先测1个真实epoch，再决定租期/是否值得迁移。

## 现时可用性和价格

通过已连接aiGalaxyCompute只读evaluate及catalog查询。provider快照时间为2026-09-24 15:30:16（未声明时区）。所有GPU的inventory_counts和quote_candidate_counts均为空；evaluate未返回可报价GPU。**当前没有已确认可租实例，也没有有效整机报价。** 不将价目表当有货。

| 候选 | GPU显存 | 价目表GPU组件价 | 3小时GPU组件小计 |
|---|---:|---:|---:|
| RTX4090 | 24GB | 1.65元/小时 | 4.95元 |
| RTX3090 | 24GB | 1.10元/小时 | 3.30元 |

CPU、内存、磁盘、带宽/镜像等须由实际plan_rental报价确认，以上**不是总价**。仅作资源规划：若按当前组件表另外计8CPU×.02、32GB×.0078125、40GB×.0006，则4090约2.084元/小时、3小时6.252元；3090约1.534元/小时、3小时4.602元。平台实际套餐、折扣、可选规格和费用覆盖可能不同，不能把此算术估计当可执行订单。

保存快照：`artifacts/e1a933_review/GPU_CATALOG_SNAPSHOT.json`。租机前必须重新evaluate并获得实际plan_rental（kind=gpu，单卡，autorenew_on=false）；规划预算建议总额10元以内，超出则重新评估，而非默认续费。该10元只是方案建议，尚非用户付费授权。

## 迁移与回收

1. 保留CPU进程及其冻结源码；独立CUDA入口和最小离线传输包另存，校验数据/预训练权重/源码哈希。不得把源码改动混入当前CPU回执。
2. 完整21臂在同一GPU协议下运行，输出新目录。CPU和GPU结果分开报告，不能拼凑有利seed或冒称逐位复现；GPU数值一致性/吞吐为NOT_RUN。
3. 租前检查真实报价、可用镜像/驱动、SSH入口、费用上限和到期方式。只迁本轮合成图像、坐标、模型及必要源码，不复制整个项目数据。
4. 有实例后先验证CUDA和真实一epoch/显存/吞吐，保存设备与软件版本；只有实测收益足够时才切换。CPU停止需在GPU稳定、回执落盘后进行，不能因报价或空启动就停。
5. 全程同一dev-only选择逻辑；断点含模型、优化器、epoch、随机流和数据合同。若不能精确恢复完整训练状态，重启该臂到新目录，不把只加载best模型当续训。
6. 完成或预算到期前回传checkpoints、逐样本预测、日志与哈希并核对；释放策略须与结果回收配合，避免无意保盘持续计费。自动续费关闭。

当前执行选择：CPU继续；GPU迁移技术包准备中。因库存未证实，暂不提出不可执行的租机确认请求。


## V100与最低资源复核（用户追问，2026-09-24）

已读取独立CUDA实现和冻结data.npz：所有numpy数组展开合计326.85MiB；224图像仅按batch32插值，不整库驻留GPU。保持ResNet18、FP32、Adam、batch32及单臂串行。

- GPU显存：预计峰值约3–5GB（含activation/workspace/辅助梯度等余量，NOT_MEASURED）；保守租用下限8GB。V10016GB充足，当前任务不需为32GB额外付费。此为工程估算，实际峰值须CUDA preflight测量。
- 主机内存：最低预算8GB，建议16GB；此前32GB为宽松配置，不是任务硬需求。数据本体不足0.4GB，剩余容纳Python/torch、插值临时量、模型副本及操作系统。
- CPU：建议8vCPU；4vCPU可运行，但CPU插值/归一化可能拖慢GPU，使下面时间估计偏乐观。
- 单V100全21臂：预计约2–3小时训练/评价，环境准备和传输合计预留3–4小时。估计条件为8vCPU及正常共享负载；没有V100实测，不能由峰值算力保证。420epochs若平均15–25秒即1.75–2.92小时；实际先用完整代表性epoch（含aux/development）替换该假设。
- NVIDIA官方V100资料列16/32GB显存与14–15.7TFLOPS FP32；本计划维持FP32，不拿FP16 Tensor Core指标估时。来源：https://images.nvidia.com/content/technologies/volta/pdf/tesla-volta-v100-datasheet-letter-fnl-web.pdf

推荐具体组合：1×V10016GB + 8vCPU + 16GB RAM；继续保留CPU任务直到GPU真实训练验证完成。目前未租用，CUDA实际显存/吞吐NOT_RUN。
