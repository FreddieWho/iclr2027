# E3 round1 — 端点规则零中途违例，但实验本身空洞（待 bigrot 版）

- 日期 2026-09-18；命令：`python3 experiments/last15h/e3_fullpath.py --out artifacts/last15h/E3/fullpath`；源 `artifacts/last15h/E3/fullpath/result.json`。
- 实测：clean/flipmine × lam{0,2}，mid_violated_rate 全 0.0（n=139）。
- 空洞诊断：全库 256 交叉场景 × 49 动作逐一验算——终点 0 且中途重入的候选动作数为 **0**（干净有效 1556 个）。线性插值从 1 到 0 在此库下几乎必然单调，mid_viol=0 是平庸真理，不是"端点规则避开了重入"。
- bigrot 版（2026-09-18；源 `E3/fullpath_bigrot/result.json`；n=213）：mid_viol 仍全 0（clean/flipmine × lam{0,2}）。
- 全库验算（含 ±0.8 大旋转）：2318 个干净有效动作，**violators 仍为 0**。线性路径从 1 到 0 在此任务族下不重入——几何事实，非样本不足。
- **判决：E3 关闭。**诚实边界结论：端点有效动作在此自动路径有效（线性插值不重入）；N03 的被动监测失败不感染目标导向的行动选择——至少在此任务/库下。N03→N06 链条需要真正可重入的动作族（如多步序列动作），列 future，不在本轮硬凑。
