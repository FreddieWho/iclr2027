# X03 round1 一页纸：反事实内部变量交换（冻结 feat 层，秩-4 补丁）

源：`artifacts/next6_ef0f7a3/x03/result.json`。hybrid=donor.AB＋base.CD；
学投影 P（32×4×2=256 参），冻 cls 读出，目标 hybrid 真值；训 300 对
（train_101），测 450 对（eval_202 新绑定，anti 150）。

## 真实结果
- clean：interchange 0.544（anti 0.147！）/ full-swap 0.491 / randsub 0.484 /
  readout 0.604（read_anti 0.540）
- flipmine：interchange 0.556（anti 0.233）/ full 0.500 / randsub 0.527 /
  readout 0.671（read_anti 0.647）
- 学出的交换 ≈ 整向量替换 ≈ 随机子空间；anti 子集上系统性答错（<0.5）＝
  在复制 base/donor 答案而非组合；同数据普通读出好得多（0.60–0.67）

## 解读
信息在（拼接 hidden 可解码 hybrid 真值），但 feat 层秩-4 补丁不可组合交换。
这是机制诊断（可解码≠可组合），正好支撑主故事，不是方法。
替代解释：训练集以非 anti 对为主，目标函数奖励复制策略——round2 排除它。

## x03b（anti-only 训练，逼组合）：硬 park，诊断转强
- clean：interchange 0.538（anti 0.273）/ readout 0.471（read_anti 0.893）
- flipmine：interchange 0.538（anti 0.233）/ readout 0.440（read_anti 0.873）
- 同一份 anti-only 数据：拼接读出 anti 0.87–0.89（模式轻易可解码），
  秩-4 交换补丁 anti 仍 0.23–0.27——信息在，但冻下游里装不上组合结构。
- 总体交换率 0.538 ≈ 整换/随机子空间，方法死。

## 判定：park（诊断保留，进讨论段）
“可解码≠可组合/可干预”是本轮最干净的阴性证据之一，正好给主故事
（原子判对不保证组合判对）加机制注脚：连监督逼出来的交换都装不上。
不修秩/换层（无机制理由再开一轮，只会稀释）。
