# U02 一页纸：决策接口 vs 表示（后续分析，eval_202，零新前向）

源：`artifacts/next6_ef0f7a3/u02/{result,cache}.npz.json`。同一候选库缓存
（logits/代价/oracle 可行性）；硬目标口径（仅 oracle 有可行动作的 93 场，
无可行场景不算模型错）；λ∈{0,1,2,5}。

## 真实结果
- 拒动率（lexico）：clean 0.43 vs flipmine 0.118——flipmine 可行性置信集
  覆盖大 3.6 倍；非拒动精度相近（0.23/0.22）→ 增益在覆盖不在精度
- score 规则：λ≥1 flipmine 全胜（0.290/0.237、0.258/0.172、0.086/0.054），
  λ0 持平 → 换规则后表示净收益保留
- 温度机制（argmax 全同 assert 通过）：T0.5/T2.0 改变 10–50% 选行动，
  成功率抖动最大 ±16pp（T2.0/lam0 flipmine +16.1pp）→ 分类决策不变，
  规划表现可大变：决策接口是独立杠杆
- 校准拟合（2026-09-18 审计修符号 bug 后重跑 artifacts/next6_ef0f7a3/u02_fixed/result.json）：
  T clean 8.14 / flipmine 8.43（旧 T→20 为 sigmoid(+lg) 拟合 class-0 标签的符号错误产物，作废）。
  T>1 ＝模型过度自信、需压平，属常规 miscalibration，无“反信息”解释。
  且 τ=0.5 时温度缩放数学上保持可行集不变 → calib≡lexico 逐数相同是设计空转，如实记 flaw

## 最强替代解释
温度/阈值收益可能只是"模型过度自信→压平即赚"，不是学到几何。
已用 dev-only 拟合＋argmax 不变约束隔离了 cherry-pick，但几何归因仍需 T5R3 侧。

## 判定：continue（做分析，不立方法）
- u02b（dev 选 τ）：clean τ=0.5、flipmine τ=0.4，测试与 lexico 逐数相同——
  阈值救不了可行集，问题在概率本身。revise 额度用完，不再开阈值变体。
- 留下做分析的三件：①决策接口是独立杠杆（argmax 不变，选行动变 10–50%，
  成功率 ±16pp）；②flipmine 净收益跨规则保留（score 全 λ＋拒动覆盖 3.6 倍）；
  ③硬目标口径（oracle 无可行不算错＋拒动单列）进论文方法段。
- 按卡边界：字典序/温度不是算法新颖性，不报方法，只报分解。
