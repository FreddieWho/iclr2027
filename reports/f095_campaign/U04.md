# U04｜冻结编码器交叉重训（2026-09-23，层间互补SUPPORTED·收窄版）

## 结果
- **P1 PASS 3/3**：relflip-encoder＋静态readout的S = 0.8734/0.9198/0.8819，与原整网S小数点后四位完全一致。排序增益在encoder里。
- **P2 PARTIAL**：flip-readout J增益仅在relfeat-encoder上为正（+.160/+.068/+.059，1/3过+0.10线）；其余encoder ≤+.025或为负。flip增益readout充分**当且仅当表示好**。
- **P3 PASS 12/12**：小MLP相对线性readout的J增益最大+.021，全格<+0.05。表示固定后线性已穷尽，额外容量无用。
- **修复结构在readout层复现**：relfeat-encoder＋flip-readout：R_full .31–.37、M .22–.27（修复主导）；raw-encoder＋flip-readout：R_full .05–.06、M .39–.48（纯迁移，J不涨）。flip监督在坏表示上只搬运错误。
- **Sanity**：静态重训readout复现原整网J（raw_clean .059/.055/.046 vs .055/.051/.055；relfeat逐格一致）；主干sha重训前后一致（脚本内断言通过）。

## 不支持什么
- 不支持"flip是通用修复"：raw-encoder上flip-readout降atomic（.52→.37/.34/.30）换AB，无J增益。
- 不支持"head容量解释一切"：P3已排除（MLP无增益）；也不支持"encoder决定一切"：relfeat上flip-readout的+.06–.16是readout层真实增益。

## 主claim落点
层间互补成立且被收窄：**S是encoder属性，J增益需要好表示×flip监督同时成立**。这正是U03三段分解的因果版本（第一段↔encoder，第三段↔readout），主文机制段可引用，附录进表。
