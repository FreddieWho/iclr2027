# D10｜下游决策前沿（2026-09-23，决策收益SUPPORTED）

## 结果（eval实测，dev选点；96场景）
- **F1 PASS**：同覆盖下flip own质量－clean，median +0.24/+0.11/+0.09（s11/s23/s47），点差为正占79–97%；paired CI下界>0占多数（21/32、12/43、11/41， acted集小导致区间宽，但点估计大而同向）。
- **F2 PASS**：同成本下median +0.25/+0.19/+0.14，正占80–88%。收益不是多花钱买来的。
- **F3 FAIL（对表示有利）**：单调校准的clean最多复制flip增益的约三分之一（s11），s23/s47约零。工作点故事不够，representation解释经此控制存活。
- **诚实项**：dev匹配的覆盖到eval漂移±0.05–0.16（v2已预警过，此处逐点公布不隐藏）；结论读法是"dev选工作点、eval测结果"，不是"eval上复现匹配"。clean最佳阈值控制退化（dev_q=1.0 collapse到eval覆盖2–9%，几乎不执行）——记为失败控制，不用它背书。
- oracle诊断上界：flip前沿最高质量点（eval标签优化）另表，仅诊断。

## 主claim落点
P2从"现象"升级为"可用"：flip策略在同覆盖、同成本下交付更高的执行集质量，且非工作点假象。主文最多一段＋一panel（D10_DESIGN原话）；机制篇幅仍归U01–U04。

## 编码器扩展（D10-ext，2026-09-23，评估-only）
- 同一 frontier 逻辑（helpers 直接 import，仅换 logit 来源）：rel12 配对中位增益 +.449/+.199/+.218（frac>0=1.0 三种子）；sixdist +.208/+.118/+.315（frac≥.947）。
- 决策收益从 raw 特例升级为跨编码器结论。产物 `D10/D10_ENC_SUMMARY.json`（d10_frontier_enc.py）。
