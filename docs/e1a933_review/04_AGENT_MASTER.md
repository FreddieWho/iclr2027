# 总控施工 Prompt：e1a933结果修复、定向升级与新路线

继续接手 `FreddieWho/iclr2027`。本轮审计基准为 `e1a933e6b32dd07c6895c7925cbccd9004da7f54`；上轮20路线基准为f095dfa。用户明确允许继续高价值探索，不得因投稿接近而把可做实验一律park。

## 任务

读本包00–03，再读仓库最新版本；如HEAD已变，先做差异审计，不能机械覆盖现场修复。按以下顺序完成：

1. 修正足以影响结果的实现、评估与解释问题；保留未受影响的结果，不全盘重跑。
2. 完善当前最强的跨任务几何输入优势与标准视觉模型桥。
3. 检验新的可观测性匹配/采样机制/决策瓶颈；选择最强1–2项进入主线。
4. 直接同步paper、claim ledger、图表来源与README/STATUS，不再让报告与主稿分裂。

## 先接受的事实

- 本轮已有15条执行、5条park；并不等于所有20条都获得科学判决。
- U10 T1、T2带flip时的距离输入优势大；但T2的原J尺度交互为负，“18/18”不能证明普遍超加性。
- U10 typed在源码中被按端点索引标准化，裸网络不变性测试不足；修完整pipeline再解释。
- U06有一个真实的目标合同问题：图像不可见的端点命名被要求准确回归；未匹配MSE的阴性不能判迁移原则失败。
- D06旧paired并非E-quartet事件；D07/U09不受该关闭判定的逻辑约束。
- D04的224输入结果是重要进步；上采样不增加观测信息，预训练效应也没有被排除。

## 执行分组（并行，而非一条全过才开下一条）

### Worker A：数据和源域/跨任务
R01、R02、R03、R04；随后O01/O02，必要时O04。先立即修影响主claim的计算与typed预处理；给视觉和足球提供共享parent/group测试函数，不阻塞它们。

### Worker B：视觉桥与新方法
R05、R06；随后O03与N01。N02小对照可并行。N03仅在匹配后的几何预测能学习、但aux未转化成任务收益时继续。旧A模型冻结为可靠基线，所有新变体另存。

### Worker C：修复公平性、决策与足球
R07、R08、R09；O05不需等视觉阳性。D05原生多模态可作为O06独立外部桥，但只使用已批准预算/权限，不擅自租机/付费。

### 汇总/写作worker
R10贯穿执行。为每个claim维护“原结果、发现问题、受影响范围、修正结果、最终措辞”五列；不中断没有问题的实验去等统一green gate。

## 现场自由度

科学问题和数据流正确性优先于预设网络/超参数。可自行改变失效优化器、role-shared normalization、匹配loss形式、抽样规模、batchsize和评估统计实现；记录为什么改变，以及改变前已经看过哪些数据。设计明显有冲突时先纠正，不机械照跑。

禁止结果导向地换分母、丢掉塌缩seed、把匹配失败变成“无效应”、用测试J选epoch。没有预先承诺的数值门槛，不临时用10pp/85%等把有信息的弱结果扔掉；同样不能把tiny effect自动升为方法。

## 最小科学合同

- 状态的命名群、任务oracle的对称群、图像可观察群三者分别定义。坐标标签允许的8群，不自动等于彩色图像不可见的4群。
- Train/dev/test先分parent或真实比赛，再生成编辑/图像/路径。大训练bank与评估bank逐模型查重。旧银行复算明确标为再分析，不假称新确认。
- 训练禁用目标评估AB、目标bank标签和根据目标J的超参数选择；train-only单编辑和既有训练任务可用。新的监督若改变合同须单独标注，不与原“没见组合”主claim混用。
- 配对比较共享same-state样本；同时报整体J、原子边际/通过率、CCM分母、110→111、迁移、111退化。不要只选择有利条件分母。
- source/target换任务时，带flip臂的均值差与2×2交互是不同问题。三种seed/两个bank/8种labeling不是独立任务数。
- 只需一次有辨识力的最终确认；工程修复、已暴露bank探索不必套层层预注册。不因确认过一次就禁止修真实bug，但修正属于修正分析，不称未见首读。
- CPU优先，使用已有机器和获批资源。高成本动作受已有授权约束；不得为避免一个询问就越过费用或数据许可。

## 可复现交付

新建 `reports/e1a933_review/` 和 `artifacts/e1a933_review/`，旧产物只读。每个任务保存：源代码commit、checkpoint哈希、data/sample哈希、实际命令、参数、逐样本分数、配对统计和结论边界。

最终必交：
- `AFFECTED_CLAIMS.csv`：结果受什么影响、是否改动结论。
- `R_FIXES_REPORT.md`：R01–R10的实际修复状态与不需要重跑的理由。
- `OPTIMIZATION_REPORT.md`：O01–O06执行/未执行和证据，不用PASS数量代替结果。
- `NEW_ROUTES_DECISION.md`：N01–N03支持/反驳/未决；每条可说与不可说的claim。
- `PAPER_PATCH_AUDIT.md`：哪些摘要/正文/图/附录更新，来源路径、计数与分母。
- `REPRODUCE.md`：真实可执行命令；使用for循环，不用`--seed {11,23,47}`当多次运行。

本包的checks是最小反例演示，不可把它们“9通过”当作原项目已修好。应将等价测试移入仓库并调用生产函数，恢复旧错误后必须失败。

## 论文即时更新要求

现在就修：T2正交互外推、typed精确性、D06不可行、D04“非先验”、U04“S是encoder属性”、D08loss归因和温度证伪工作点等过强语句。有效数字不删。

最终故事由新结果选择，不能预设所有工作拼成一条因果链。优先争取：
“任务适配的表示使相同变化监督更容易转化成完整联合正确；这种优势跨具体几何关系存在，但交互形态受任务和基线能力限制；视觉迁移还要求监督目标与可观察信息相容。”

只有N01/O03等真的成立才加入后半句。若主要新claim未成立，保留已验证的跨任务优势及视觉强基线，不硬写新机制。
