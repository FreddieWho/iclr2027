# 给总控Agent：e832887收缩主线与定向升级

仓库：FreddieWho/iclr2027；基准 e832887c938948b23e8783719d493f2b9b8c3a17。
本轮用户明确：先不考虑投稿时间，允许GPU。不要引用旧文档“永久关闭”来阻断本轮明确授权；也不要把GPU许可理解为无限租机或已授权新账户支出。

## 最终目标

形成一篇只有一个主问题、两条主实验线的完整论文：
**哪些表示与关系计算条件，让single-edit监督改善完整组合正确性，而不只是搬移错误？**

主线A：纠正表达能力对照，查清L015/距离输入的收益来自什么。
主线B：把同一个问题放到已胜任任务的ResNet18上，做直接、同口径的验证。
条件性C：只有它能够解释视觉结论时，才继续分离网格/算力效应。

## 先读与先纠错

阅读当前paper、FINAL_COMPLETION_MATRIX、L015_L006_REPORT、O01_DATA_OPTIMIZATION_REPORT、VISION_GPU_RESULTS、N02_SAMPLING_ACCESS_REPORT。
立即核对06中的源任务段级加性结构。用原仓库类运行微型棋盘样例；不是只跑包内镜像类。
保留旧checkpoint与结果，标记旧typed对照的表达能力限制。不同任务typed结构逐个确认，不连坐。

## 执行组织

- A worker：02；CPU足够时不占GPU，允许GPU批处理。
- B worker：03；先统一既有视觉证据，再执行直接相关的模型对照。
- 写作/证据worker：05；从第一天同步修改正文和ledger。
- C worker：默认不启动长训练，只读现有结果并完成MAC/数据审计；达到04的价值判断后再训练。

建议新增目录（不是声称仓库已有）：
experiments/e832_focus/{structure,vision,sampling}/
artifacts/e832_focus/{structure,vision,sampling}/
reports/e832_focus/

## 公共测量

1. 主指标J3=P(A、B、AB全对|E)，保留原151/176分母历史；J4含base只作补充。
2. 修复同时报R_endpoint、R_full、M及baseline111退化。跨模型比较固定各自baseline或共同来源并说明。
3. 同时保存原子边际/联合准确率、终点准确率、各模型自身CCM、固定基线子集、S和J*诊断。
4. parent配对重采样；seed变异另报，不把10seed当10数据集，也不把8个群元当独立重复。
5. 训练无目标测试AB/四元组监督；single train/dev用于选型。别为“没有AB训练”禁止普通同域独立状态，它是合理强基线；以样本生成历史而非图像长相定义是否joint训练。
6. 所有实验共享原始状态/编辑清单；按角色和原始坐标审计，不能靠整数ID去重。
7. 归一化、renderer、子集、优化器、模型初始化分别记录。数据RNG独立于建模、arm排序和aux层数。
8. 新颖性评估用开发数据探索充分；最终只为最强的1–2个claim做新parent/renderer验证，不为每个小消融设置封存门。

## 自由度

科学问题 > 正确性 > 现场数据 > 本文档的局部结构/阈值建议。
可以改优化器、宽度、统计估计、实验顺序；记录一段理由和是否已看测试结果。
不要求所有seed同号或固定10pp才算有信息。用效应、区间、复现范围与反例决定claim等级。
如果简单标准基线解决了问题，应更新文章解释；不临时提高难度以保住失败率。

## GPU

优先使用已授权GPU；单卡24GB足以作为ResNet18/中等交互head的资源起点，具体batch按实测显存确定并冻结。
所有候选在相同可比较backend内跑；FP32/AMP作为配置记录，做最小数值parity，不要求不同硬件逐位一致。
预算记训练前向/MAC、选中checkpoint、总搜索成本和推理MAC；不只匹配参数量或epoch。
新增付费合同、凭据或租机支出超出已有授权时单独报告；不把每个正常训练配置都变成人工审批节点。

## 不再扩张

O05足球、O06原生VLM、P2部署、N01裸aux、N03自由瓶颈暂入边界/附录，不再各自开大矩阵。
P1不作因果中介，不强求它在每一新模型复现。
非主线意外lead记LEADS.md；最多一个低成本反驳测试，不自动形成下一campaign。

## 最终输出

- STRUCTURE_DECISION.md：旧对照何处受限、修后怎样、因素如何区分。
- VISUAL_DECISION.md：同一主张是否在胜任的视觉系统成立，而不是只报更高准确率。
- SAMPLING_DECISION.md：没启动也交价值判断；启动则给预算前沿与混杂边界。
- CLAIM_LEDGER.csv/JSON：来源、分母、单位、数据使用历史、允许措辞。
- PAPER_MIGRATION.md + 直接修改paper：只让最强的1–2项新增结论占正文。
- REPRODUCE.md：一键数据/模型/分析入口，重量文件可以外置但必须真可获取，不用hash替代数据。

最终允许：强机制实例 / 有效但机制未清 / 标准基线解决 / 有界负面结果。禁止“工程跑完=科学SUPPORTED”。
